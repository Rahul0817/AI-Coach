# ---------------------------------------------------------------- web layer
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
sse-starlette==2.2.1

# ------------------------------------------------------------- config/models
pydantic==2.10.5
pydantic-settings==2.7.1
email-validator==2.2.0

# ----------------------------------------------------------------- database
sqlalchemy[asyncio]==2.0.37
asyncpg==0.30.0
psycopg2-binary==2.9.10          # sync driver, used by Alembic only
alembic==1.14.1
greenlet==3.1.1

# -------------------------------------------------------------------- cache
redis==5.2.1

# ----------------------------------------------------------------- security
passlib[bcrypt]==1.7.4
# Pinned below 4.1 deliberately. passlib 1.7.4 reads `bcrypt.__about__.__version__`
# during backend detection, and bcrypt removed that attribute in 4.1. Hashing
# still works — passlib traps the error — but it prints a full traceback on every
# process start, which is noise that trains operators to ignore stack traces.
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

# ------------------------------------------------------------ ML / analytics
numpy==2.2.1
pandas==2.2.3
scikit-learn==1.6.1
xgboost==2.1.3
joblib==1.4.2
shap==0.46.0
matplotlib==3.10.0               # ROC / feature-importance plots during training

# --------------------------------------------------------------- AI and RAG
openai==1.59.6
chromadb==0.6.3
tiktoken==0.8.0

# ------------------------------------------------------------- multimodal IO
pillow==11.1.0
pytesseract==0.3.13
pypdf==5.1.0
pdf2image==1.17.0

# ------------------------------------------------------------------ storage
cloudinary==1.42.1

# --------------------------------------------------------------------- misc
httpx==0.28.1
python-dateutil==2.9.0.post0
tenacity==9.0.0
